<Application x:Class="NetworkService.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Linux Terminal Theme Colors -->
        <SolidColorBrush x:Key="BackgroundDark" Color="#0a0a0a"/>
        <SolidColorBrush x:Key="BackgroundMedium" Color="#1a1a1a"/>
        <SolidColorBrush x:Key="BackgroundLight" Color="#2a2a2a"/>
        <SolidColorBrush x:Key="BorderColor" Color="#3a3a3a"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#ffffff"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#888888"/>
        <SolidColorBrush x:Key="TextTerminal" Color="#00ff00"/>
        <SolidColorBrush x:Key="StatusOnline" Color="#00ff00"/>
        <SolidColorBrush x:Key="StatusWarning" Color="#ffaa00"/>
        <SolidColorBrush x:Key="StatusOffline" Color="#ff0000"/>

        <!-- Font Awesome Style -->
        <Style x:Key="FontAwesome" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="/Fonts/#Font Awesome 5 Free Solid"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Global Styles -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        </Style>
    </Application.Resources>
</Application>